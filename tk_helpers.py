# Simple TKinter - tk_helpers.py
# layout helper

# Nested 2D for over all components in layout
def create(layout):
    for r in range(len(layout)):
        for c in range(len(layout[r])):
            obj = layout[r][c]
            if obj:
                obj.grid(row=r, column=c)
























# 1. Helper movido para arquivo tk_helpers.py
# 2. Método renomeado para create
# 3.
# 4.
# 5.
# 6.
# 7.
# 8.
# 9. 
